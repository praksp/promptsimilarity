# Tests: point to an invalid port so "unavailable" tests get connection refused (500).
# WireMock test class overrides this via QuarkusTestResource to return 200.
quarkus.rest-client.prompt-service.url=http://127.0.0.1:19999
