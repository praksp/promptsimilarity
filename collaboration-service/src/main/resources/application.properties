quarkus.application.name=collaboration-service
# HTTP port (health, future REST)
quarkus.http.port=8080

# Dedicated gRPC server (used by prompt-service)
quarkus.grpc.server.use-separate-server=true
quarkus.grpc.server.port=9005

# Teams/Slack (production)
# collaboration.teams.client-id=
# collaboration.slack.token=
