quarkus.application.name=vector-service
# HTTP port (health, future REST)
quarkus.http.port=8080

# Dedicated gRPC server (used by prompt-service)
quarkus.grpc.server.use-separate-server=true
quarkus.grpc.server.port=9002

# Production: qdrant.host=qdrant
# qdrant.port=6334
